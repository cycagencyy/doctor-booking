{
  "version": 2,
  "buildCommand": "npm ci --prefix client || npm install --prefix client && npm run build --prefix client",
  "outputDirectory": "client/build",
  "functions": {
    "api/*.js": { "runtime": "nodejs18.x" }
  }
}
